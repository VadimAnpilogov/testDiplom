function whichDialog() {
    var dialogsMenu = document.getElementById("widget_message");
    var dialogs = document.getElementsByClassName("message_list");
    var url = document.location.pathname;
    // alert(url);
}
whichDialog();